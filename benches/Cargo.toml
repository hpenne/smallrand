[package]
name = "benches"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]

[dev-dependencies]
smallrand = { path = ".." }
criterion = "0.5"
criterion-cycles-per-byte = "0.6"

[[bench]]
name = "generators"
harness = false
